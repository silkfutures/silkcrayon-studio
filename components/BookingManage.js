"use client";import {useEffect,useMemo,useState} from 'react';
const iso=d=>`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;function days(n=14){const a=[],d=new Date();d.setDate(d.getDate()+1);while(a.length<n){if(d.getDay()!==0)a.push(new Date(d));d.setDate(d.getDate()+1)}return a}
export default function BookingManage({booking,cutoffHours}){const quick=useMemo(()=>days(),[]),[date,setDate]=useState(booking.booking_date),[slots,setSlots]=useState([]),[slot,setSlot]=useState(null),[msg,setMsg]=useState(''),[busy,setBusy]=useState(false),[note,setNote]=useState('');useEffect(()=>{fetch(`/api/availability?date=${date}&service=${booking.service_slug}&duration=${booking.duration_minutes}`).then(r=>r.json()).then(j=>setSlots(j.slots||[])).catch(()=>setSlots([]));setSlot(null)},[date]);async function action(body){setBusy(true);setMsg('');const r=await fetch(`/api/customer/bookings/${booking.id}`,{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify(body)});const j=await r.json();setBusy(false);if(!r.ok)return setMsg(j.error||'Could not update booking.');if(body.action==='reschedule'){setMsg('Session moved ✓');setTimeout(()=>location.href='/account',700)}else{setMsg('Cancellation request sent ✓');setTimeout(()=>location.reload(),700)}}return <div className="manageBooking"><section className="managePanel"><p className="eyebrow">Reschedule</p><h2>Choose another slot.</h2><p className="muted">Online changes close {cutoffHours} hours before your session.</p><div className="dateGrid">{quick.map(d=><button type="button" key={iso(d)} className={`dateCard ${date===iso(d)?'selected':''}`} onClick={()=>setDate(iso(d))}><span>{d.toLocaleDateString('en-GB',{weekday:'short'})}</span><b>{d.getDate()}</b><small>{d.toLocaleDateString('en-GB',{month:'short'})}</small></button>)}</div><div className="slotGrid manageSlots">{slots.map(s=><button type="button" className={`slot ${slot?.start===s.start?'selected':''}`} key={s.start} onClick={()=>setSlot(s)}>{s.start}</button>)}</div><button className="button primary" disabled={!slot||busy} onClick={()=>action({action:'reschedule',date,start:slot.start})}>Move my session →</button></section><section className="managePanel cancelPanel"><p className="eyebrow">Can’t make it?</p><h2>Request cancellation.</h2><p className="muted">This sends the studio a cancellation request. It does not automatically issue a card refund.</p>{booking.cancellation_requested_at?<div className="portalNotice"><b>Cancellation requested.</b><span>The studio will contact you about the next step.</span></div>:<><textarea value={note} onChange={e=>setNote(e.target.value)} rows="3" placeholder="Optional note for the studio"/><button className="button outline" disabled={busy} onClick={()=>action({action:'cancel_request',note})}>Request cancellation</button></>}{msg&&<p className="manageMessage">{msg}</p>}</section></div>}
