'use client';import {useMemo,useState} from 'react';import {useRouter} from 'next/navigation';import ArtistSearchSelect from './ArtistSearchSelect';
export default function MixJobForm({customers=[]}){const router=useRouter(),[customerId,setCustomerId]=useState(''),[tracks,setTracks]=useState([{title:''}]),[quotedPrice,setQuotedPrice]=useState(''),[paymentMode,setPaymentMode]=useState('unpaid'),[paidAmount,setPaidAmount]=useState(''),[busy,setBusy]=useState(false),[msg,setMsg]=useState('');const remaining=useMemo(()=>{const total=Number(quotedPrice),received=paymentMode==='paid'?total:Number(paidAmount);if(!Number.isFinite(total)||total<0)return null;return Math.max(0,total-(Number.isFinite(received)?received:0))},[quotedPrice,paidAmount,paymentMode]);async function submit(e){e.preventDefault();setBusy(true);setMsg('');const f=new FormData(e.currentTarget),body={customerId,tracks:tracks.map(t=>t.title),serviceType:f.get('serviceType'),quotedPrice,paymentMode,paidAmount:paymentMode==='partial_paid'?paidAmount:null,paymentMethod:paymentMode==='unpaid'?null:f.get('paymentMethod'),paymentReference:paymentMode==='unpaid'?null:f.get('paymentReference'),paymentDueDate:paymentMode==='paid'?null:f.get('paymentDueDate'),filesReceived:f.get('filesReceived')==='on',referenceTracks:f.get('referenceTracks'),notes:f.get('notes'),vocalTuningEditing:f.get('vocalTuningEditing')==='on',stemDelivery:f.get('stemDelivery')==='on'};const r=await fetch('/api/admin/mixes',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)}),j=await r.json().catch(()=>({}));setBusy(false);if(!r.ok)return setMsg(j.error||'Could not create mix.');router.push(`/admin/mixes/${j.id}`)}return <section className="adminSection"><p className="eyebrow">New internal job</p><h2>Create a mix</h2><p className="muted">Save the job with its real payment position. No customer message is sent when you create it.</p><form className="mixJobForm" onSubmit={submit}><div className="field wide"><span>Customer</span><ArtistSearchSelect customers={customers} value={customerId} onChange={setCustomerId}/></div><div className="field wide"><span>Tracks</span><div className="mixTrackBuilder">{tracks.map((t,i)=><div className="mixTrackRow" key={i}><b>{String(i+1).padStart(2,'0')}</b><input required value={t.title} onChange={e=>setTracks(x=>x.map((a,n)=>n===i?{title:e.target.value}:a))} placeholder="Track title"/><button type="button" disabled={tracks.length===1} onClick={()=>setTracks(x=>x.filter((_,n)=>n!==i))}>Remove</button></div>)}<button type="button" className="button outline" onClick={()=>setTracks(x=>[...x,{title:''}])}>+ Add another track</button></div></div><div className="mixFormGrid"><label className="field"><span>Service</span><select name="serviceType"><option value="mix_only">Mix only</option><option value="mix_master">Mix + master</option><option value="master_only">Master only</option><option value="not_sure">Decide after review</option></select></label><label className="field"><span>Overall quote (£)</span><input name="quotedPrice" type="number" min="0" step="0.01" required value={quotedPrice} onChange={e=>setQuotedPrice(e.target.value)}/></label><label className="field"><span>Payment status</span><select value={paymentMode} onChange={e=>{setPaymentMode(e.target.value);setPaidAmount('')}}><option value="unpaid">Payment due / not paid yet</option><option value="paid">Paid already</option><option value="partial_paid">Deposit / part paid</option></select><small>{paymentMode==='paid'?'The full quote will be recorded as received and the mix goes straight to Ready to start.':paymentMode==='partial_paid'?'Record what has arrived; only the remaining balance stays outstanding.':'The full quote stays outstanding until you request or record payment.'}</small></label>{paymentMode!=='paid'&&<label className="field"><span>Payment due</span><input name="paymentDueDate" type="date"/></label>}{paymentMode==='partial_paid'&&<label className="field"><span>Already received (£)</span><input name="paidAmount" type="number" min="0.01" step="0.01" required value={paidAmount} onChange={e=>setPaidAmount(e.target.value)}/><small>{remaining===null?'Enter the quote first.':`Remaining after deposit: £${remaining.toFixed(2)}`}</small></label>}{paymentMode!=='unpaid'&&<><label className="field"><span>Payment method</span><select name="paymentMethod" defaultValue="bank_transfer"><option value="bank_transfer">Monzo / bank transfer</option><option value="cash">Cash</option><option value="card">Card paid separately</option><option value="manual">Other manual payment</option></select></label><label className="field"><span>Payment reference (optional)</span><input name="paymentReference" maxLength="200" placeholder="Monzo reference, invoice note, etc."/></label></>}<label className="checkRow"><input name="filesReceived" type="checkbox"/><span>Files received</span></label><label className="checkRow"><input name="vocalTuningEditing" type="checkbox"/><span>Vocal tuning / editing</span></label><label className="checkRow"><input name="stemDelivery" type="checkbox"/><span>Stem delivery</span></label></div><label className="field"><span>Reference tracks</span><textarea name="referenceTracks"/></label><label className="field"><span>Internal notes</span><textarea name="notes"/></label><button className="button primary" disabled={busy||!customerId}>{busy?'Saving…':paymentMode==='paid'?'Create as paid →':paymentMode==='partial_paid'?'Create with deposit →':'Save internally →'}</button>{msg&&<small>{msg}</small>}</form></section>}
