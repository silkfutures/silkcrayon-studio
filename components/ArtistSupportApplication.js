'use client';import {useState} from 'react';
export default function ArtistSupportApplication(){const [busy,setBusy]=useState(false),[msg,setMsg]=useState('');async function submit(e){e.preventDefault();setBusy(true);const body=Object.fromEntries(new FormData(e.currentTarget).entries()),r=await fetch('/api/artist-support',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)}),j=await r.json().catch(()=>({}));setBusy(false);if(r.ok){e.currentTarget.reset();setMsg('Application received. We’ll review it and come back to you about fit, scope and price.')}else setMsg(j.error||'Could not send application.')}return <form className="artistSupportForm" onSubmit={submit}><div className="mixFormGrid"><label className="field"><span>Your name</span><input name="fullName" required/></label><label className="field"><span>Artist name</span><input name="artistName"/></label><label className="field"><span>Email</span><input name="email" type="email" required/></label><label className="field"><span>Mobile</span><input name="phone"/></label><label className="field wide"><span>Music and artist links</span><textarea name="musicLinks" rows="3" placeholder="Spotify, SoundCloud, private links and socials"/></label><label className="field wide"><span>What are you trying to achieve?</span><textarea name="goals" rows="4" required/></label><label className="field wide"><span>What is currently stopping you?</span><textarea name="blockers" rows="4" required/></label><label className="field"><span>Unreleased songs</span><input name="backlog" placeholder="e.g. 12 demos, 3 finished songs"/></label><label className="field"><span>Ideal release timeframe</span><input name="timeframe"/></label><label className="field wide"><span>What support would help most?</span><textarea name="supportNeeded" rows="4" placeholder="Planning, accountability, distribution, release management, content coordination…"/></label></div><button className="button primary" disabled={busy}>{busy?'Sending…':'Apply for ongoing support →'}</button>{msg&&<p className="deliveryMessage">{msg}</p>}</form>}
