# V20.2.1 — Customer portal access hotfix

- Adds a compatibility `esc` alias to the shared notification escaping helper.
- Customer portal magic-link email generation no longer has any undefined `esc` reference path.
- `/api/customer/access` logs mail-provider failures without turning the login request into an application 500.
- No SQL changes beyond the existing V20.2 combined launch migration.
