# V20.5.1 — Public promotions route hotfix

Fixes Vercel/Turbopack build failure:

`Export activePromotions doesn't exist in target module`

The stale `app/api/public/promotions/route.js` imported a non-existent
`activePromotions` export. This replacement uses the actual V20.5 exports:

- `getLivePromotions()`
- `publicPromotion()`

It returns only the public promotion projection.

Replace the existing file with this patch and redeploy.
