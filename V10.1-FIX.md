# V10.1 hotfix

Fixes the customer portal HTTP 500 caused by undefined email-template helper names in V10.

Also adds graceful magic-link failure handling so expired/broken links return customers to the branded login screen instead of a browser 500 page.

No new Supabase migration or environment variables are required beyond V10.
