# V20.2.6.1 — Build hotfix

Fixed:
- malformed timezone helper in `/api/customer/bookings/[id]` that caused the Turbopack parse failure
- explicit ESM package type removes Node MODULE_TYPELESS_PACKAGE_JSON warning
- 30p test booking restored safely behind `/booking?test=1`
- server still requires `ENABLE_SYSTEM_TEST_BOOKING=true` before it will create a 30p checkout

No SQL changes from V20.2.6.

Migration testing:
1. Set `ENABLE_SYSTEM_TEST_BOOKING=true` in the Vercel environment being tested.
2. Open `/booking?test=1`.
3. The normal public `/booking` page does not show the test product.
4. Before domain launch set `ENABLE_SYSTEM_TEST_BOOKING=false`.
