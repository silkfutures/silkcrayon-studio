import {NextResponse} from 'next/server';
import {getStaffContext} from '../../../../lib/auth';
import {getAdminDb} from '../../../../lib/supabase';
export async function POST(req){try{const ctx=await getStaffContext();if(!ctx||ctx.profile.role!=='owner')return NextResponse.json({error:'Owner access required.'},{status:403});const b=await req.json();const price=Math.round(Number(b.studioFinishPrice)*100),revisions=Math.round(Number(b.studioFinishRevisions)),turnaround=String(b.studioFinishTurnaround||'').trim();if(!Number.isFinite(price)||price<30||price>1000000)return NextResponse.json({error:'Enter a valid Studio Finish price.'},{status:400});if(!Number.isFinite(revisions)||revisions<0||revisions>20)return NextResponse.json({error:'Revisions must be between 0 and 20.'},{status:400});if(!turnaround||turnaround.length>80)return NextResponse.json({error:'Enter a turnaround of 80 characters or fewer.'},{status:400});const rows=[{key:'studio_finish_price_pence',value:String(price)},{key:'studio_finish_turnaround',value:turnaround},{key:'studio_finish_revisions',value:String(revisions)}];const {error}=await getAdminDb().from('studio_settings').upsert(rows,{onConflict:'key'});if(error)throw error;return NextResponse.json({ok:true});}catch(e){return NextResponse.json({error:e.message||'Could not save settings.'},{status:500})}}