import { NextResponse } from 'next/server';
import { getStaffContext } from '../../../../../lib/auth';
import { getAdminDb } from '../../../../../lib/supabase';
export async function PATCH(req,{params}){try{const ctx=await getStaffContext();if(!ctx||ctx.profile.role!=='owner')return NextResponse.json({error:'Owner access required.'},{status:403});const {id}=await params;const b=await req.json();if(id===ctx.user.id&&b.active===false)return NextResponse.json({error:'You cannot deactivate your own owner account.'},{status:400});const db=getAdminDb();const patch={updated_at:new Date().toISOString()};if(typeof b.active==='boolean')patch.active=b.active;if(b.role&&['owner','engineer'].includes(b.role))patch.role=b.role;const {data,error}=await db.from('staff_profiles').update(patch).eq('user_id',id).select().single();if(error)throw error;return NextResponse.json(data);}catch(e){return NextResponse.json({error:e.message},{status:500})}}
