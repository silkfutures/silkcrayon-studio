import { NextResponse } from 'next/server';
import { getStaffContext } from '../../../../../lib/auth';
import { getAdminDb } from '../../../../../lib/supabase';
export async function PATCH(req,{params}){try{const ctx=await getStaffContext();if(!ctx||ctx.profile.role!=='owner')return NextResponse.json({error:'Owner access required.'},{status:403});const {id}=await params;const b=await req.json();if(id===ctx.user.id&&b.active===false)return NextResponse.json({error:'You cannot deactivate your own owner account.'},{status:400});const db=getAdminDb();const {data:target}=await db.from('staff_profiles').select('role,active').eq('user_id',id).maybeSingle();const patch={updated_at:new Date().toISOString()};
if(target?.role==='owner'&&target?.active&&(b.active===false||b.role==='engineer')){const {count}=await db.from('staff_profiles').select('user_id',{count:'exact',head:true}).eq('role','owner').eq('active',true);if(Number(count||0)<=1)return NextResponse.json({error:'Silkcrayon must always have at least one active owner.'},{status:409});}if(typeof b.active==='boolean')patch.active=b.active;if(b.role&&['owner','engineer'].includes(b.role))patch.role=b.role;const {data,error}=await db.from('staff_profiles').update(patch).eq('user_id',id).select().single();if(error)throw error;return NextResponse.json(data);}catch(e){return NextResponse.json({error:e.message},{status:500})}}
