import { NextResponse } from 'next/server';
import { getStaffContext } from '../../../../lib/auth';
import { getAdminDb } from '../../../../lib/supabase';
export async function POST(req){try{const ctx=await getStaffContext();if(!ctx||ctx.profile.role!=='owner')return NextResponse.json({error:'Owner access required.'},{status:403});const b=await req.json();if(!b.fullName||!b.email||!b.password)return NextResponse.json({error:'Name, email and password are required.'},{status:400});if(String(b.password).length<10)return NextResponse.json({error:'Temporary password must be at least 10 characters.'},{status:400});if(!['owner','engineer'].includes(b.role))return NextResponse.json({error:'Invalid role.'},{status:400});const db=getAdminDb();const {data,error}=await db.auth.admin.createUser({email:b.email,password:b.password,email_confirm:true,user_metadata:{full_name:b.fullName}});if(error)throw error;const {error:pe}=await db.from('staff_profiles').insert({user_id:data.user.id,full_name:b.fullName,engineer_name:b.engineerName||b.fullName,role:b.role,active:true});if(pe){await db.auth.admin.deleteUser(data.user.id);throw pe;}return NextResponse.json({ok:true,id:data.user.id});}catch(e){return NextResponse.json({error:e.message},{status:500})}}
