import {NextResponse} from 'next/server';import {getStaffContext} from '../../../../lib/auth';import {getAdminDb} from '../../../../lib/supabase';import {initialMixPaymentState} from '../../../../lib/mixPayment';
export async function POST(req){try{const ctx=await getStaffContext();if(!ctx||ctx.profile.role!=='owner')return NextResponse.json({error:'Owner access required.'},{status:403});const b=await req.json(),quoted=Math.round(Number(b.quotedPrice)*100),tracks=(Array.isArray(b.tracks)?b.tracks:[b.trackTitle]).map(x=>String(x||'').trim()).filter(Boolean);if(!b.customerId||!tracks.length||!Number.isFinite(quoted)||quoted<0)return NextResponse.json({error:'Customer, at least one track and a valid quote are required.'},{status:400});const requestedPaid=Math.round(Number(b.paidAmount)*100),now=new Date().toISOString();let payment;try{payment=initialMixPaymentState({quotedAmountPence:quoted,paymentMode:b.paymentMode||'unpaid',paidAmountPence:Number.isFinite(requestedPaid)?requestedPaid:0,paymentMethod:b.paymentMethod||'bank_transfer',paymentReference:b.paymentReference},now)}catch(e){return NextResponse.json({error:e.message},{status:400})}const db=getAdminDb(),{data,error}=await db.from('mix_jobs').insert({customer_id:b.customerId,track_title:tracks[0],service_type:b.serviceType||'mix_only',quoted_amount_pence:quoted,...payment,payment_due_date:payment.status==='ready_to_start'?null:(b.paymentDueDate||null),files_received:Boolean(b.filesReceived),reference_tracks:String(b.referenceTracks||'').trim()||null,notes:String(b.notes||'').trim()||null,vocal_tuning_editing:Boolean(b.vocalTuningEditing),stem_delivery:Boolean(b.stemDelivery),created_by_user_id:ctx.user.id}).select('id').single();if(error)throw error;await db.from('mix_tracks').delete().eq('mix_job_id',data.id);const {error:te}=await db.from('mix_tracks').insert(tracks.map((title,position)=>({mix_job_id:data.id,title,position})));if(te){await db.from('mix_jobs').delete().eq('id',data.id);throw te}const outstanding=Math.max(0,quoted-payment.paid_amount_pence),activity=[{mix_job_id:data.id,event_type:'job_created',channel:'internal',status:'recorded',detail:`Saved internally with ${tracks.length} track${tracks.length===1?'':'s'}. No customer message sent.`,created_by_user_id:ctx.user.id}];if(payment.paid_amount_pence>0)activity.push({mix_job_id:data.id,event_type:'payment_received',channel:'manual',status:'recorded',detail:outstanding===0?`Full payment recorded on creation: £${(quoted/100).toFixed(2)} · ${payment.payment_method}${payment.payment_reference?' · '+payment.payment_reference:''}. Pipeline: ready to start.`:`Deposit / part payment recorded on creation: £${(payment.paid_amount_pence/100).toFixed(2)} of £${(quoted/100).toFixed(2)} · £${(outstanding/100).toFixed(2)} outstanding · ${payment.payment_method}${payment.payment_reference?' · '+payment.payment_reference:''}.`,created_by_user_id:ctx.user.id});await db.from('mix_activity').insert(activity);return NextResponse.json({id:data.id,paymentStatus:outstanding===0&&quoted>0?'paid':payment.paid_amount_pence>0?'partial_paid':'unpaid',paidAmountPence:payment.paid_amount_pence,outstandingPence:outstanding})}catch(e){return NextResponse.json({error:e.message||'Could not create mix job.'},{status:500})}}
