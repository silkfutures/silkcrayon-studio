import {NextResponse} from 'next/server';
import {getStaffContext} from '../../../../lib/auth';
import {getAdminDb} from '../../../../lib/supabase';
const ALLOWED=new Set(['unknown','subscribed','never_subscribed']);
export async function POST(req){try{const ctx=await getStaffContext();if(!ctx)return NextResponse.json({error:'Staff access required.'},{status:403});const b=await req.json();if(!b.fullName?.trim()||!b.email?.trim()||!b.artistName?.trim())return NextResponse.json({error:'Artist name, full name and email are required.'},{status:400});if(!b.harmfulMusicPolicy)return NextResponse.json({error:'No Harmful Music Policy acceptance is required.'},{status:400});const emailStatus=ALLOWED.has(b.emailMarketingStatus)?b.emailMarketingStatus:'unknown',smsStatus=ALLOWED.has(b.smsMarketingStatus)?b.smsMarketingStatus:'unknown',email=b.email.trim().toLowerCase(),db=getAdminDb();const {data:existing}=await db.from('customers').select('id').eq('email',email).maybeSingle();if(existing)return NextResponse.json({error:'A customer with this email already exists.',id:existing.id},{status:409});const {data,error}=await db.from('customers').insert({artist_name:b.artistName.trim(),full_name:b.fullName.trim(),email,phone:b.phone?.trim()||null,postcode:b.postcode?.trim().toUpperCase()||null,area:b.area?.trim()||null,preferred_genre:b.genre?.trim()||null,instagram:b.instagram?.trim()||null,date_of_birth:b.dateOfBirth||null,goals:b.goals?.trim()||null,marketing_source:b.marketingSource?.trim()||null,marketing_consent:emailStatus==='subscribed',sms_marketing_consent:smsStatus==='subscribed',harmful_music_policy_accepted:true,policy_accepted_at:new Date().toISOString(),registered_by_user_id:ctx.user.id}).select('id').single();if(error)throw error;const now=new Date().toISOString();const {error:crmError}=await db.from('crm_contacts').update({marketing_status:emailStatus,marketing_consent:emailStatus==='subscribed',sms_marketing_status:smsStatus,sms_marketing_consent:smsStatus==='subscribed',email_consent_recorded_at:emailStatus==='subscribed'?now:null,email_consent_source:emailStatus==='subscribed'?'manual_customer_creation':null,sms_consent_recorded_at:smsStatus==='subscribed'?now:null,sms_consent_source:smsStatus==='subscribed'?'manual_customer_creation':null,updated_at:now}).eq('customer_id',data.id);if(crmError)throw crmError;return NextResponse.json(data)}catch(e){return NextResponse.json({error:e.message},{status:500})}}
