import {NextResponse} from "next/server"; import {getAdminDb} from "../../../lib/supabase";
export async function POST(r){try{const b=await r.json(); await getAdminDb().from("page_views").insert({path:String(b.path||"/").slice(0,500),referrer:String(b.referrer||"").slice(0,500),visitor_id:String(b.visitorId||"").slice(0,100)}); return NextResponse.json({ok:true});}catch{return NextResponse.json({ok:false})}}
