import AdminNav from '../../../components/AdminNav';import MarketingCentre from '../../../components/MarketingCentre';import {requireOwner} from '../../../lib/auth';import {getAdminDb} from '../../../lib/supabase';export const dynamic='force-dynamic';
export default async function Marketing(){const ctx=await requireOwner(),db=getAdminDb();const [{count:eligible},{data:campaigns=[]}]=await Promise.all([db.from('crm_contacts').select('id',{count:'exact',head:true}).eq('marketing_status','subscribed').eq('marketing_consent',true).not('email','is',null),db.from('marketing_campaigns').select('*').order('created_at',{ascending:false}).limit(30)]);return <main className="adminPage"><header className="adminHeader"><div><p className="eyebrow">Growth</p><h1>Marketing.</h1><p className="muted">Write once here, send through Resend, and keep unsubscribe handling out of your head.</p></div><AdminNav profile={ctx.profile}/></header><MarketingCentre eligible={eligible||0} campaigns={campaigns}/></main>}
