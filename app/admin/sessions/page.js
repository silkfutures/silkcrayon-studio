import AdminNav from '../../../components/AdminNav'; import SessionReportForm from '../../../components/SessionReportForm'; import {getAdminDb} from '../../../lib/supabase'; import {formatGBP} from '../../../lib/services'; export const dynamic='force-dynamic';
export default async function Sessions(){const db=getAdminDb();const [{data:bookings=[]},{data:reports=[]}]=await Promise.all([db.from('bookings').select('*,customers(full_name,artist_name)').gte('booking_date',new Date(Date.now()-86400000).toISOString().slice(0,10)).order('booking_date').order('start_time'),db.from('session_reports').select('*').order('session_date',{ascending:false}).limit(100)]);return <main className="adminPage"><header className="adminHeader"><div><p className="eyebrow">Silkcrayon OS</p><h1>Sessions</h1></div><AdminNav/></header><section className="adminSection"><p className="eyebrow">Engineer workflow</p><h2>Complete a session report</h2><p className="muted">Replaces the old engineer Google Form. Select a booking to prefill the artist, date, time and session value.</p><SessionReportForm bookings={bookings}/></section><section className="adminSection"><p className="eyebrow">History</p><h2>Recent reports</h2><div className="tableWrap"><table><thead><tr><th>Date</th><th>Artist</th><th>Engineer</th><th>Hours</th><th>Studio value</th><th>Engineer pay</th><th>Follow-up</th></tr></thead><tbody>{reports.map(r=><tr key={r.id}><td>{r.session_date}</td><td><b>{r.artist_name}</b></td><td>{r.engineer}</td><td>{r.actual_hours}</td><td>{formatGBP(r.studio_fee_pence)}</td><td>{formatGBP(r.engineer_fee_pence)}</td><td>{r.follow_up||'—'}</td></tr>)}</tbody></table></div></section></main>}
