import Link from 'next/link';import {requireOwner} from '../../../../../lib/auth';import {getAdminDb} from '../../../../../lib/supabase';
export const dynamic='force-dynamic';
export default async function MixSources({params}){await requireOwner();const {id}=await params,db=getAdminDb(),[{data:j},{data:files=[]}]=await Promise.all([db.from('mix_jobs').select('track_title,client_brief,reference_tracks,genre,bpm,musical_key,release_date').eq('id',id).single(),db.from('mix_source_files').select('*').eq('mix_job_id',id).order('created_at')]);return <main className="adminPage"><header className="adminHeader"><div><p className="eyebrow">Source review</p><h1>{j?.track_title||'Mix files'}</h1><p className="muted">Inspect the submission before setting the quote.</p></div><Link className="button outline" href={`/admin/mixes/${id}`}>← Back to mix</Link></header><section className="adminSection"><div className="profileGrid"><div><small>Genre</small><b>{j?.genre||'—'}</b></div><div><small>BPM / key</small><b>{j?.bpm||'—'} · {j?.musical_key||'—'}</b></div><div><small>Release date</small><b>{j?.release_date||'—'}</b></div><div className="wide"><small>Client brief</small><p>{j?.client_brief||'—'}</p></div><div className="wide"><small>References</small><p>{j?.reference_tracks||'—'}</p></div></div></section><section className="adminSection"><h2>Uploaded files</h2>{files.length?files.map(f=><a className="clientDelivery" href={`/api/admin/mixes/${id}/sources/${f.id}`} key={f.id}><div><b>{f.file_name}</b><small>{f.file_kind.replace('_',' ')} · {f.size_bytes?`${(f.size_bytes/1048576).toFixed(1)} MB`:''}</small></div><span>Download securely →</span></a>):<p className="muted">No uploaded files were recorded.</p>}</section></main>}
