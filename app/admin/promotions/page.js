import {requireOwner} from '../../../lib/auth';import {getAdminDb} from '../../../lib/supabase';import AdminNav from '../../../components/AdminNav';import PromotionAdmin from '../../../components/PromotionAdmin';
export const dynamic='force-dynamic';
export default async function Promotions(){const ctx=await requireOwner();const {data=[],error}=await getAdminDb().from('promotions').select('*').order('priority',{ascending:false});return <main className="adminPage"><header className="adminHeader"><div><p className="eyebrow">Studio OS · Commercial</p><h1>Promotions.</h1><p className="muted">Control public offers without changing code or redeploying.</p></div><AdminNav profile={ctx.profile}/></header><section className="adminSection">{error?<div className="errorBox">Run the V20.5 promotions migration first: {error.message}</div>:<PromotionAdmin initial={data}/>}</section></main>}
